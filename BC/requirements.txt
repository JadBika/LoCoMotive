# BC fine-tuning and data processing (Mac / Apple Silicon)
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.0.0
torchvision>=0.15.0

# ViNT dependencies
efficientnet-pytorch
lmdb

# Warmup LR scheduler (not on PyPI — install from source)
# pip install git+https://github.com/ildoonet/pytorch-gradual-warmup-lr.git

# Data processing
rosbags>=0.10
numpy
Pillow
tqdm

# Plotting
matplotlib
